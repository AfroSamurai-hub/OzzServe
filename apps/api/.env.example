# OzzServe API Environment Variables
# These should be set in GCP Cloud Run Secret Manager or directly in Environment Variables.

# Database Configuration (Cloud SQL)
# When using Cloud SQL Auth Proxy on Cloud Run, use /cloudsql/CONNECTION_NAME as the host.
DB_USER=ozzserve_user
DB_PASS=password_here
DB_NAME=ozzserve
DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_ID

# Firebase Configuration
# Stringified JSON of the service account key
FIREBASE_CONFIG={"type": "service_account", "project_id": "...", ...}

# Server Settings
NODE_ENV=production
PORT=8080
